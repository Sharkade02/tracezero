<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\TraceZero.Domain\TraceZero.Domain.csproj" />
    <ProjectReference Include="..\TraceZero.Engine\TraceZero.Engine.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Surface minimale : ce helper n'existe que pour exécuter une commande élevée puis s'arrêter. -->
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AssemblyName>TraceZero.Elevated</AssemblyName>
  </PropertyGroup>

</Project>
